global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'backend'
    metrics_path: /api/health
    static_configs:
      - targets: ['backend:3001']
  - job_name: 'prometheus'
    static_configs:
      - targets: ['prometheus:9090']
